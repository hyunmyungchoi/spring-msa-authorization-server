spring:
  config:
    activate:
      on-profile: docker

  data:
    redis:
      host: redis
      port: 6379

  security:
    oauth2:
      authorizationserver:
        issuer: http://spring-cloud-gateway:8080

app:
  user-service:
    base-url: "http://spring-user-service:8081"